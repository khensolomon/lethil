const sassMiddleware = require('node-sass-middleware');
// module.exports = function(options){
//   return sassMiddleware(options);
// }
// module.exports = (options) => sassMiddleware(options);
module.exports = sassMiddleware;
